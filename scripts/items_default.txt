# One items bitmask per line. Blank lines and lines starting with `#` are ignored.
# Trailing inline comments (after a `#`) are also stripped.
137438953472   # units (mirrors .github/workflows/launch_comlink_and_get_game_data.yml)
